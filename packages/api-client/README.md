# ReviewOrbit API client

This package is reserved for generated TypeScript bindings from the Laravel OpenAPI contract. Generated source is committed and checked for drift in CI once the first authenticated resources are introduced; handwritten domain logic does not belong here.

